# SummayGenAi

python -m venv env
source env/bin/activate
pip install torch transformers accelerate bitsandbytes sentencepiece protobuf pandas tqdm

pip install pandas torch tqdm scikit-learn sentence-transformers transformers accelerate einops futures glob2